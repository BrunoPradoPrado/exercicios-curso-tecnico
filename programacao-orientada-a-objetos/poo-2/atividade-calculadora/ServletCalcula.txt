

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class ServletCalcula
 */
@WebServlet("/ServletCalcula")
public class ServletCalcula extends HttpServlet {
	private static final long serialVersionUID = 1L;
       
    /**
     * @see HttpServlet#HttpServlet()
     */
    public ServletCalcula() {
        super();
        // TODO Auto-generated constructor stub
    }

	/**
	 * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		response.getWriter().append("Served at: ").append(request.getContextPath());
	}

	/**
	 * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)
	 */
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		// TODO Auto-generated method stub
		doGet(request, response);
		String sNumero = request.getParameter("txNum");
		String sNumeroII = request.getParameter("txNumII");
		String sSinal = request.getParameter("txSin");
		
		float fNumero = Float.parseFloat(sNumero);
		float fNumeroII = Float.parseFloat(sNumeroII);
		float fResultado = 0;
		if (sSinal.equals("+")){
			fResultado = fNumero + fNumeroII;
		} else if (sSinal.equals("-")) {
			fResultado = fNumero - fNumeroII;
		} else if (sSinal.equals("*")) {
			fResultado = fNumero * fNumeroII;
		} else if (sSinal.equals("/")) {
			fResultado = fNumero / fNumeroII;
		}
		String sResultado = Float.toString(fResultado);
		response.setContentType("text/html");
		PrintWriter pw = response.getWriter();
		pw.println("<h1>Resultado: "+sResultado+"</h1>");

	}

}
